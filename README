[automation 내용]
LINUX CentOS 7.0 기준 version
LINUX Ubuntu 16.04 version 추가
20170307 README 수정 

[hosts 값 지정]
/etc/ansible/hosts 에 hosts variable 추가

ex)
[test]
xxx.xxx.xxx.xxx

[files 다운로드]
ANSIBLE_HOME 지정 : ANSIBLE_HOME=/root/...

scp -r root@192.168.2.194:/root/ansible_binary $ANSIBLE_HOME/files

[automation 사용]

./new_run.sh -h								help
./new_run.sh -H [host] command				host를 입력하고 command를 실행      
ex) command 예
.  ./new_run.sh -H [host] init				initial 설정만 진행
.  ./new_run.sh -H [host] rundbmanual		tibero를 수동 설치 실행
.  ./new_run.sh -H [host] rundb				docker container를 이용한 설치 실행
.  ./new_run.sh -H [host] installpo			install PO7
.  ./new_run.sh -H [host] runpo				IaaS master setting & run PO7
.  ./new_run.sh -H [host] runcompute		LXC setting 및 설치
.  ./new_run.sh -H [host] runstorage		Setting zfs pool
.  ./new_run.sh -H [host] test				test 실행
.  ./new_run.sh -H [host] delete			원격 host내에 모든 설정과 파일, docker를 삭제
.  ./new_run.sh -H [host] deletedb			Tibero6 manual로 설치한 것 삭제
.  ./new_run.sh -H [host] cleannode			Compute node 설정 초기화(container삭제)
   ./new_run.sh -H [host] deletestorage		Delete zfs pool

[automation execute sequence]
init rundb
init installpo
init runpo
init runcompute
init runstorage

[automation에서 사용하고 있는 yml]

auto_run_init.yml
auto_run_db.yml
auto_run_po.yml
auto_run_test.yml
auto_run_delete.yml
